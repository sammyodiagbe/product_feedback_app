import "./App.css";
import { BrowserRouter as Router, Routes, Route } from "react-router-dom";

function App() {
  return;
  <Router>
    <div className="product-app-container">
      <Routes>{/* the routes go here */}</Routes>
    </div>
  </Router>;
}

export default App;
